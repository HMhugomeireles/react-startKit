import React from 'react'
import ReactDOM from 'react-dom'

ReactDOM.render(
  <div>
    <h1>Start Kit</h1>
    <p>The start kit to build app in react</p>
  </div>,
  document.getElementById('app')
);